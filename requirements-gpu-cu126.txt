# RAPIDS/CUDA overlay for the shared A100 host venv.
# Repair this stack with `uv pip --no-deps` against the existing
# `/home/ubuntu/.venv`; it is not a from-scratch lockfile for resolving
# torch and RAPIDS together.
cupy-cuda12x==14.0.1
cuml-cu12==26.2.0
cuda-python==12.9.4
cuda-bindings==12.9.4
cuda-pathfinder==1.3.3
rmm-cu12==26.2.0
pylibraft-cu12==26.2.0
