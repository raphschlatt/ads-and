stage: "mini"
subset_target_mentions: 10000
seed: 11
subset_sampling:
  target_mean_block_size: 4
max_pairs_per_block: 100000
split_balance:
  min_neg_val: 50
  min_neg_test: 50
  max_attempts: 300
train_seeds: [1, 2, 3]
